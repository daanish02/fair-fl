name: fedavg_synthetic
seed: 0
data: {name: synthetic, num_clients: 6, alpha: 0.5, partition_on: sensitive}
model: {kind: mlp, hidden: [16]}
train: {rounds: 10, clients_per_round: 1.0, local_epochs: 1, batch_size: 64, lr: 0.1}
strategy: {name: fedavg}
fairness:
  status: {benefit: dp, level: global, accumulation: cumulative}
  scope: {kind: anytime}
  epsilon: 0.05
